# FIXME (no fix needed) - the solution has some redundant code lines, the if statement is incorrect

echo "Welcome ${USER}"
apt install npm

npm outdated
stat=true

if $.stat -c "%a" $.token !=false  &&  $.token != -r -w  ; then
  echo "Warning: .token file has too open permissions"
  fi

   input "$usr"
   adduser "$usr"
   su -l "$usr"
